import styled from 'styled-components';
import styles from 'styles/styleLib';

const Type = styled.p`
  margin: 0 0 5px 0;
  color: ${styles.colors.emphColor};
  font-size: 14px;
  line-height: 160%;
  text-align: left;
`;

export default Type;
